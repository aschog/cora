[project]
name = "cora-api"
version = "0.1.0"
description = "cora's contract: what the problem is made of, and the slots technology fills. Stdlib only"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.26,<0.12.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = ["cora.domain", "cora.ports"]

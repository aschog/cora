[server]
# Match the ingestion cap (DEFAULT_MAX_BYTES in packages/engine/src/cora/engine/ingestion.py)
# so the uploader advertises and enforces the same 10 MB limit it will accept.
maxUploadSize = 10

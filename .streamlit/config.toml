[server]
# Match the ingestion cap (DEFAULT_MAX_BYTES in packages/cora-core/src/cora/core/services/ingestion.py)
# so the uploader advertises and enforces the same 10 MB limit it will accept.
maxUploadSize = 10

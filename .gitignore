# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/

# Virtual environments
.venv/

# Tooling caches
.pytest_cache/
.ruff_cache/
node_modules/

# Environment / secrets
.env

# OS
.DS_Store

# Local vector store (Chroma persistence)
.cora/
# Everything cora keeps at runtime is ignored — except the folder a plugin is dropped
# into, which has to be there to drop one into.
!.cora/plugins/
.cora/plugins/*
!.cora/plugins/.gitkeep
e2e-artifacts/

# The built docs site.
site/

# The editor's own workspace.
.obsidian/

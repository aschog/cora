No test list. The subject of this change is one written page, and a `.md` file is not
test-backed — it is held by a person reading it. One claim is machine-readable and has
one assertion: the page's list of what cora keeps, read against the settings, so a store
that moves is red until the page names it. It was written after the page rather than
before it, because the page is what discovered that the locations guard alone holds
nothing here — every store is made at runtime, so naming one as a directory would claim
a path a clean checkout does not have.

Done is the page answering the story's criteria, checked in review against
`specs/disclosure/spec.md` — and the criterion that matters most cannot be automated at
all, because it asks whether the claims are *true*.

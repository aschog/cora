# Tasks

The pages are prose, held by a person reading them. What a test can hold is a name.

## 1. What the docs name, cora has

- [x] 1.1 Write a guard test that a documentation page naming a `CORA_*` variable no
      configuration reads fails, and see it fail on a page that names one — proved by
      reintroducing `CORA_PLUGIN_FITNESS_UNITS`, which reads as *fitness does not read
      'units'*.
- [x] 1.2 Write a guard test that every `make` target the docs name is one the
      `Makefile` defines.

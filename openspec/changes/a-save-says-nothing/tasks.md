## 1. The outer test

- [ ] 1.1 Write the test that a save cora took writes no line of its own on the strip, marked `@pytest.mark.xfail(strict=True)`.

## 2. Close it

- [ ] 2.1 Drop the marker and watch it pass, the browser suites reading the save off the field.

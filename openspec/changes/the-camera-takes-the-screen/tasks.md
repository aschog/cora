## 1. The outer test

Chromium is given a fake camera for this spec, the only one that needs it.

- [ ] 1.1 Write a browser test that the opened camera fills the page, the header, the row, the sets and the weight drawn over it, marked `test.fail()` while in flight.

## 2. What stays in the frame

- [ ] 2.1 Write a browser test that the mirror control and the set count sit between the exercise row and the sets while the picture fills the page.

## 3. Close it

- [ ] 3.1 Drop the marker and watch the outer test pass.

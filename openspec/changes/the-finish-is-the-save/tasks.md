## 1. The outer test

- [x] 1.1 Write the test that the page keeps no history of its own and reads the field's documents instead, marked `@pytest.mark.xfail(strict=True)`.

## 2. The coach

- [ ] 2.1 Write the test that a document named for its moment and its workout is a session of that day.

## 3. The page

- [ ] 3.1 Write the test that a save is named for its moment and then for the workout, and for the moment alone without one.
- [ ] 3.2 Write the browser test that History lists a saved workout's exercises, load and reps, read from the field.
- [ ] 3.3 Write the browser test that a fresh workout opens on the weight and reps of the field's latest save of that exercise.
- [ ] 3.4 Write the browser test that a save cora refused leaves the sets logged and the finish offered, the strip saying why.
- [ ] 3.5 Write the test that Import, Export, the textarea and the clipboard are gone from the page.
- [ ] 3.6 Write the test that the page fetches the sheet, the notice, the listing and a document, and posts to cora alone.

## 4. Close it

- [ ] 4.1 Drop the marker and watch the outer test pass.

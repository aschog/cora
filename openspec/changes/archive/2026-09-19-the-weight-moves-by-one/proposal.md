## Why

The weight moves four kilograms a tap, so a bell between two steps cannot be logged as it is.

## What Changes

- The trainer's weight moves one kilogram a tap, and never below one
- Capability `plugins` — how the weight is adjusted is stated

## Impact

- `plugins/fitness/src/cora/plugins/fitness/page/index.html` — the step and the floor
- `plugins/fitness/tests/` — the step asserted
- Left alone: what a save writes, the sheet's weights, and cora

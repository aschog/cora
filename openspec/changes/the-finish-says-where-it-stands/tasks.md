## 1. The outer tests

- [x] 1.1 Write the test that a save cora took writes no line of its own on the strip, marked `@pytest.mark.xfail(strict=True)`.
- [x] 1.2 Write the test that the button reads no sets logged yet, finish, or saved, and that a logged set clears saved, marked `@pytest.mark.xfail(strict=True)`.

## 2. Close it

- [x] 2.1 Drop the markers and watch both pass, the browser suites reading the button.

## 1. The outer test

- [x] 1.1 Write the functional test where a tool keeps a value and the plugin's answer handler reads it in the same turn, marked `@pytest.mark.xfail(strict=True)`.

## 2. Bound alike

- [x] 2.1 Write a test that an answer handler reads what a tool kept this turn.
- [x] 2.2 Write a test that what an answer handler kept is read by a tool on the next turn.
- [x] 2.3 Write a test that another plugin's handler reads nothing of it.

## 3. Close it

- [x] 3.1 Drop the marker and watch the outer test pass.

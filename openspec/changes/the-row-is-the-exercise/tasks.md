## 1. The outer test

- [x] 1.1 Write the test that a row carries no sets count and no weight, marked `@pytest.mark.xfail(strict=True)`.

## 2. Close it

- [ ] 2.1 Drop the marker and watch it pass, the browser suite reading progress off the sets.
